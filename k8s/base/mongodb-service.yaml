apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: devsecops
  labels:
    app: mongodb
    app.kubernetes.io/name: mongodb
    app.kubernetes.io/part-of: devsecops-mean
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP
