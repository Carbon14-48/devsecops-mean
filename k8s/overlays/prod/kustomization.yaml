apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: devsecops

patches:
  - target:
      kind: Deployment
      name: devsecops-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 3

commonLabels:
  environment: production
